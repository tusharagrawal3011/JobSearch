# Copy to `.env.local`. Base URL of the local FastAPI backend (backend/api/main.py).
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000
# Optional: shown in the sidebar.
NEXT_PUBLIC_OWNER_NAME=your local job-search assistant
